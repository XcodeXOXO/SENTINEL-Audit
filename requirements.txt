# ── Three-Pillar RAG / Orchestration Stack ────────────────────────────────────
langchain>=0.2.0
langchain-groq>=0.1.0
langchain-google-genai>=1.0.0
langgraph>=0.1.0
chromadb>=0.5.0
pydantic>=2.7.0
python-dotenv>=1.0.0
requests>=2.31.0
jinja2>=3.1.0

# ── Solidity / Audit Tooling ──────────────────────────────────────────────────
solidity-parser>=0.1.1
py-solc-x>=2.0.0
beautifulsoup4>=4.12.0

# ── Tokenisation ──────────────────────────────────────────────────────────────
tiktoken>=0.7.0
transformers>=4.40.0

# ── Phase 4: LoRA Inference Engine (GPU / Colab) ──────────────────────────────
# NOTE: unsloth must be installed via its own pip command in Colab:
#   pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
unsloth
torch>=2.2.0
peft>=0.10.0
bitsandbytes>=0.43.0
accelerate>=0.30.0
hf_transfer>=0.1.6

# ── Phase 4: CLI & Evaluation UI ─────────────────────────────────────────────
rich>=13.7.0
tqdm>=4.66.0
tabulate>=0.9.0